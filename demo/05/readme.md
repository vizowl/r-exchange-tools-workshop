# Demo/05

> renv to the rescue

- I added ggthemes, but my colleague hasn't installed it
- How can we make sure our team stays in sync?
- [renv](https://rstudio.github.io/renv/)
- `install.packages('renv')`
- `renv::init()`
- `renv::restore()` on the other side
